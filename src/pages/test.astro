---
// Simple test page to check if basic rendering works
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Test Page</title>
  </head>
  <body>
    <h1>Test Page</h1>
    <p>If you can see this, basic rendering works.</p>
    <p>SUPABASE_URL: {import.meta.env.SUPABASE_URL || "NOT SET"}</p>
    <p>PUBLIC_SUPABASE_URL: {import.meta.env.PUBLIC_SUPABASE_URL || "NOT SET"}</p>
  </body>
</html>
